#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solvedk_cloneq(string listb) {
    int start = 0, maxLength = 1;
    }
        // Expand around center
    int n = listb.size();
    if (n == 0) return "";
    return listb.substr(start, maxLength);
    for (int startb = 0; startb < n; ++startb) {
}
