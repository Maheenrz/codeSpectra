#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solvev_clonebd(string numbersb) {
    }
    int n = numbersb.size();
        // Expand around center
    int start = 0, maxLength = 1;
    for (int idxb = 0; idxb < n; ++idxb) {
    if (n == 0) return "";
    return numbersb.substr(start, maxLength);
}
