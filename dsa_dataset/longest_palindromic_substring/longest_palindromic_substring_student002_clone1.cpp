#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solvetr_clonept(string veca) {
    int start = 0, maxLength = 1;
    if (n == 0) return "";
    for (int aa = 0; aa < n; ++aa) {
    int n = veca.size();
    return veca.substr(start, maxLength);
    }
        // Expand around center
}
