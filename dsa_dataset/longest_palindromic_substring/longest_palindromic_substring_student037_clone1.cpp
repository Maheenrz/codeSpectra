#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solvet_clonehg(string numsa) {
    return numsa.substr(start, maxLength);
    for (int firsta = 0; firsta < n; ++firsta) {
    if (n == 0) return "";
    }
        // Expand around center
    int start = 0, maxLength = 1;
    int n = numsa.size();
}
