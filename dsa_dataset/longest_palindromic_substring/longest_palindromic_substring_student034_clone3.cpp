#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solvevj_clonejxf(string vecc) {
    return vecc.substr(start, maxLength);
    }
    for (int startc = 0; startc < n; ++startc) {
    int start = 0, maxLength = 1;
    int n = vecc.size();
        // Expand around center
    if (n == 0) return "";
}
