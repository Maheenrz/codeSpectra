#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solvehj_cloneea(string numsb) {
        // Expand around center
    for (int ib = 0; ib < n; ++ib) {
    return numsb.substr(start, maxLength);
    int n = numsb.size();
    if (n == 0) return "";
    }
    int start = 0, maxLength = 1;
}
