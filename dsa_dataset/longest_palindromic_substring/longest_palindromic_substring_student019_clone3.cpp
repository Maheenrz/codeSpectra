#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solvek_clonekug(string numsc) {
    for (int startc = 0; startc < n; ++startc) {
    }
    return numsc.substr(start, maxLength);
        // Expand around center
    int n = numsc.size();
    if (n == 0) return "";
    int start = 0, maxLength = 1;
}
