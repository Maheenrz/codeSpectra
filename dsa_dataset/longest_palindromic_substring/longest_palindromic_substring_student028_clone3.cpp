#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solvelo_clonepu(string numsc) {
        // Expand around center
    if (n == 0) return "";
    int start = 0, maxLength = 1;
    }
    int n = numsc.size();
    return numsc.substr(start, maxLength);
    for (int idxc = 0; idxc < n; ++idxc) {
}
