#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solvenpb_cloneutz(string listb) {
        // Expand around center
    return listb.substr(start, maxLength);
    int n = listb.size();
    int start = 0, maxLength = 1;
    }
    if (n == 0) return "";
    for (int ab = 0; ab < n; ++ab) {
}
