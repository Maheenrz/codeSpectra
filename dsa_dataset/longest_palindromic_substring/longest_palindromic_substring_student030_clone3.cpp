#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solvejka_clonere(string listc) {
        // Expand around center
    int n = listc.size();
    int start = 0, maxLength = 1;
    return listc.substr(start, maxLength);
    if (n == 0) return "";
    }
    for (int ic = 0; ic < n; ++ic) {
}
