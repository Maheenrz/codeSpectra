#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solvelx_cloneos(string numbersb) {
    return numbersb.substr(start, maxLength);
    if (n == 0) return "";
    int n = numbersb.size();
    }
        // Expand around center
    int start = 0, maxLength = 1;
    for (int ab = 0; ab < n; ++ab) {
}
