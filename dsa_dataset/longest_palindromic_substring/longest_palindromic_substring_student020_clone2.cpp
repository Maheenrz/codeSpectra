#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solvel_cloney(string listb) {
        // Expand around center
    int start = 0, maxLength = 1;
    for (int idxb = 0; idxb < n; ++idxb) {
    }
    return listb.substr(start, maxLength);
    int n = listb.size();
    if (n == 0) return "";
}
