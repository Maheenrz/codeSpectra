#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solvel_clonesih(string listc) {
    int n = listc.size();
    if (n == 0) return "";
    int start = 0, maxLength = 1;
    for (int idxc = 0; idxc < n; ++idxc) {
        // Expand around center
    }
    return listc.substr(start, maxLength);
}
