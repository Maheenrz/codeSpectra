#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solveib_cloner(string vecb) {
    return vecb.substr(start, maxLength);
    if (n == 0) return "";
    }
        // Expand around center
    int start = 0, maxLength = 1;
    for (int firstb = 0; firstb < n; ++firstb) {
    int n = vecb.size();
}
