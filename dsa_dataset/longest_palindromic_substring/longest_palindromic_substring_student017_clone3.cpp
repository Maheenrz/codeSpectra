#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solvemt_cloneluc(string datasetc) {
    }
    for (int ac = 0; ac < n; ++ac) {
    return datasetc.substr(start, maxLength);
    if (n == 0) return "";
        // Expand around center
    int start = 0, maxLength = 1;
    int n = datasetc.size();
}
