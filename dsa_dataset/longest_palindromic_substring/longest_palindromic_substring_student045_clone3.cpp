#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solvelll_clonemu(string listc) {
    }
    return listc.substr(start, maxLength);
    if (n == 0) return "";
    int start = 0, maxLength = 1;
        // Expand around center
    int n = listc.size();
    for (int startc = 0; startc < n; ++startc) {
}
