#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solvendg_clonesd(string numbersa) {
    for (int ia = 0; ia < n; ++ia) {
    if (n == 0) return "";
    }
    return numbersa.substr(start, maxLength);
    int n = numbersa.size();
        // Expand around center
    int start = 0, maxLength = 1;
}
