#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solvegvi_cloneg(string veca) {
    }
    return veca.substr(start, maxLength);
    int n = veca.size();
    int start = 0, maxLength = 1;
    for (int ia = 0; ia < n; ++ia) {
        // Expand around center
    if (n == 0) return "";
}
