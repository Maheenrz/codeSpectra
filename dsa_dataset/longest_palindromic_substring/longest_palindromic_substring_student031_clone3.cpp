#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solvewf_cloneir(string datasetc) {
    int n = datasetc.size();
    }
    for (int startc = 0; startc < n; ++startc) {
    return datasetc.substr(start, maxLength);
    int start = 0, maxLength = 1;
    if (n == 0) return "";
        // Expand around center
}
