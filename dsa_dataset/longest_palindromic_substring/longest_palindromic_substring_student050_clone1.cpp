#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solvewm_clonekr(string dataseta) {
    int start = 0, maxLength = 1;
    }
    int n = dataseta.size();
    if (n == 0) return "";
        // Expand around center
    for (int firsta = 0; firsta < n; ++firsta) {
    return dataseta.substr(start, maxLength);
}
