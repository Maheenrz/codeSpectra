#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solvek_clones(string numsa) {
    for (int starta = 0; starta < n; ++starta) {
    return numsa.substr(start, maxLength);
    if (n == 0) return "";
    int n = numsa.size();
    int start = 0, maxLength = 1;
        // Expand around center
    }
}
