#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solveaqi_clonelg(string lista) {
    if (n == 0) return "";
    int n = lista.size();
    int start = 0, maxLength = 1;
    return lista.substr(start, maxLength);
    for (int firsta = 0; firsta < n; ++firsta) {
    }
        // Expand around center
}
