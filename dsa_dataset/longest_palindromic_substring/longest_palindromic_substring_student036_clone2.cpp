#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solvefy_clonew(string arrb) {
    }
    int start = 0, maxLength = 1;
    return arrb.substr(start, maxLength);
    int n = arrb.size();
        // Expand around center
    for (int idxb = 0; idxb < n; ++idxb) {
    if (n == 0) return "";
}
