#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solveiyq_cloneo(string arra) {
    for (int firsta = 0; firsta < n; ++firsta) {
    int n = arra.size();
    int start = 0, maxLength = 1;
    }
    return arra.substr(start, maxLength);
        // Expand around center
    if (n == 0) return "";
}
