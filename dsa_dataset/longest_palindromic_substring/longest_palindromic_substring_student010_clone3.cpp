#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solveu_clonepyh(string numbersc) {
    int n = numbersc.size();
    if (n == 0) return "";
    int start = 0, maxLength = 1;
    for (int startc = 0; startc < n; ++startc) {
        // Expand around center
    }
    return numbersc.substr(start, maxLength);
}
