#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solveiyq_clonergd(string arrb) {
    }
    for (int firstb = 0; firstb < n; ++firstb) {
    if (n == 0) return "";
    int start = 0, maxLength = 1;
    return arrb.substr(start, maxLength);
        // Expand around center
    int n = arrb.size();
}
