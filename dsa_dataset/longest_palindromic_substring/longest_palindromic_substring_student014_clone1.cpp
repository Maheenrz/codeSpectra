#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solvemuh_clonep(string dataseta) {
        // Expand around center
    }
    int start = 0, maxLength = 1;
    if (n == 0) return "";
    return dataseta.substr(start, maxLength);
    int n = dataseta.size();
    for (int idxa = 0; idxa < n; ++idxa) {
}
