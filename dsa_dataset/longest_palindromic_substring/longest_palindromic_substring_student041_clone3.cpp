#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solveov_clonenpe(string arrc) {
    for (int idxc = 0; idxc < n; ++idxc) {
    }
        // Expand around center
    if (n == 0) return "";
    return arrc.substr(start, maxLength);
    int start = 0, maxLength = 1;
    int n = arrc.size();
}
