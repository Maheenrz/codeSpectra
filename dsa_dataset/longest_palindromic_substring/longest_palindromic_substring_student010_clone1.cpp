#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solveu_cloneu(string numbersa) {
        // Expand around center
    return numbersa.substr(start, maxLength);
    if (n == 0) return "";
    for (int starta = 0; starta < n; ++starta) {
    int start = 0, maxLength = 1;
    }
    int n = numbersa.size();
}
