#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solvendg_clonef(string numbersb) {
    int start = 0, maxLength = 1;
        // Expand around center
    }
    return numbersb.substr(start, maxLength);
    int n = numbersb.size();
    if (n == 0) return "";
    for (int ib = 0; ib < n; ++ib) {
}
