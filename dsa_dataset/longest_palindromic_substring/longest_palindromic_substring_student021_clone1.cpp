#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solvetdz_cloney(string dataseta) {
    int n = dataseta.size();
    if (n == 0) return "";
    int start = 0, maxLength = 1;
    for (int ia = 0; ia < n; ++ia) {
        // Expand around center
    }
    return dataseta.substr(start, maxLength);
}
