#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solvejka_cloneco(string lista) {
    if (n == 0) return "";
    int n = lista.size();
    for (int ia = 0; ia < n; ++ia) {
    }
        // Expand around center
    return lista.substr(start, maxLength);
    int start = 0, maxLength = 1;
}
