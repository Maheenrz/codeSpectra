#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solvehj_clonedbc(string numsa) {
    return numsa.substr(start, maxLength);
    if (n == 0) return "";
    }
    for (int ia = 0; ia < n; ++ia) {
    int start = 0, maxLength = 1;
    int n = numsa.size();
        // Expand around center
}
