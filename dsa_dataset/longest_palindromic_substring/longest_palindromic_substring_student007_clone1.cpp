#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solveacb_cloness(string lista) {
    int start = 0, maxLength = 1;
    return lista.substr(start, maxLength);
    }
        // Expand around center
    int n = lista.size();
    if (n == 0) return "";
    for (int starta = 0; starta < n; ++starta) {
}
