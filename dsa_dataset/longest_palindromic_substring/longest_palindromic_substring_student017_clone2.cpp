#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solvemt_clonekcj(string datasetb) {
    int start = 0, maxLength = 1;
    }
    int n = datasetb.size();
    if (n == 0) return "";
    return datasetb.substr(start, maxLength);
        // Expand around center
    for (int ab = 0; ab < n; ++ab) {
}
