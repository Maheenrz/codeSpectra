#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solvehj_clonesd(string numsc) {
    return numsc.substr(start, maxLength);
    if (n == 0) return "";
    }
        // Expand around center
    int start = 0, maxLength = 1;
    int n = numsc.size();
    for (int ic = 0; ic < n; ++ic) {
}
