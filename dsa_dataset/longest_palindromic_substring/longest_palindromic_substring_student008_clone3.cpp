#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solvelx_clonep(string numbersc) {
    return numbersc.substr(start, maxLength);
        // Expand around center
    int start = 0, maxLength = 1;
    if (n == 0) return "";
    for (int ac = 0; ac < n; ++ac) {
    int n = numbersc.size();
    }
}
