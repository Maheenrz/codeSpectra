#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solvec_clonegka(string numsa) {
    }
    int start = 0, maxLength = 1;
    if (n == 0) return "";
    int n = numsa.size();
        // Expand around center
    return numsa.substr(start, maxLength);
    for (int aa = 0; aa < n; ++aa) {
}
