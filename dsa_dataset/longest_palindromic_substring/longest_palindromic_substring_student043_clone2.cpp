#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solveuct_clonemzh(string listb) {
    int start = 0, maxLength = 1;
    return listb.substr(start, maxLength);
    if (n == 0) return "";
        // Expand around center
    }
    for (int ib = 0; ib < n; ++ib) {
    int n = listb.size();
}
