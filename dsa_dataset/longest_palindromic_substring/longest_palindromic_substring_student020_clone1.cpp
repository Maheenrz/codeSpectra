#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solvel_clonenuw(string lista) {
    int n = lista.size();
    return lista.substr(start, maxLength);
    if (n == 0) return "";
    for (int idxa = 0; idxa < n; ++idxa) {
    }
    int start = 0, maxLength = 1;
        // Expand around center
}
