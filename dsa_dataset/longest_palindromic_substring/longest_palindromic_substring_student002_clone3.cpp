#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solvetr_cloneql(string vecc) {
    int start = 0, maxLength = 1;
    int n = vecc.size();
        // Expand around center
    if (n == 0) return "";
    for (int ac = 0; ac < n; ++ac) {
    }
    return vecc.substr(start, maxLength);
}
