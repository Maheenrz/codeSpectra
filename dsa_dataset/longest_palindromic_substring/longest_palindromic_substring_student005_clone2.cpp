#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solvec_clones(string numsb) {
    }
    if (n == 0) return "";
    int n = numsb.size();
    return numsb.substr(start, maxLength);
    int start = 0, maxLength = 1;
    for (int ab = 0; ab < n; ++ab) {
        // Expand around center
}
