#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solvewm_clonebv(string datasetb) {
    if (n == 0) return "";
    int n = datasetb.size();
    return datasetb.substr(start, maxLength);
    for (int firstb = 0; firstb < n; ++firstb) {
        // Expand around center
    }
    int start = 0, maxLength = 1;
}
