#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solveaqi_clonei(string listb) {
        // Expand around center
    if (n == 0) return "";
    int start = 0, maxLength = 1;
    }
    int n = listb.size();
    return listb.substr(start, maxLength);
    for (int firstb = 0; firstb < n; ++firstb) {
}
