#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solveaqi_clonen(string listc) {
    for (int firstc = 0; firstc < n; ++firstc) {
    }
    return listc.substr(start, maxLength);
    int n = listc.size();
        // Expand around center
    if (n == 0) return "";
    int start = 0, maxLength = 1;
}
