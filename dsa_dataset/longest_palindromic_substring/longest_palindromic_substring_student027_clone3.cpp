#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solveea_clonelv(string vecc) {
    for (int idxc = 0; idxc < n; ++idxc) {
        // Expand around center
    int start = 0, maxLength = 1;
    if (n == 0) return "";
    }
    return vecc.substr(start, maxLength);
    int n = vecc.size();
}
