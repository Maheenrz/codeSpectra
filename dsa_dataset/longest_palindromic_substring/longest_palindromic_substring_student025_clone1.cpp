#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solvev_clonec(string numbersa) {
        // Expand around center
    int n = numbersa.size();
    for (int idxa = 0; idxa < n; ++idxa) {
    int start = 0, maxLength = 1;
    if (n == 0) return "";
    return numbersa.substr(start, maxLength);
    }
}
