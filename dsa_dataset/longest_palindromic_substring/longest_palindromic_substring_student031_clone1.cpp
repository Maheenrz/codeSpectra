#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solvewf_clonek(string dataseta) {
    int start = 0, maxLength = 1;
    return dataseta.substr(start, maxLength);
        // Expand around center
    int n = dataseta.size();
    if (n == 0) return "";
    }
    for (int starta = 0; starta < n; ++starta) {
}
