#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solvemrr_clonecl(string veca) {
    int n = veca.size();
    for (int idxa = 0; idxa < n; ++idxa) {
    if (n == 0) return "";
        // Expand around center
    }
    int start = 0, maxLength = 1;
    return veca.substr(start, maxLength);
}
