#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solvenpb_clonedr(string listc) {
    return listc.substr(start, maxLength);
    if (n == 0) return "";
    for (int ac = 0; ac < n; ++ac) {
    int start = 0, maxLength = 1;
        // Expand around center
    }
    int n = listc.size();
}
