#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solvetr_clonehcj(string vecb) {
    int n = vecb.size();
    if (n == 0) return "";
    int start = 0, maxLength = 1;
    for (int ab = 0; ab < n; ++ab) {
        // Expand around center
    }
    return vecb.substr(start, maxLength);
}
