#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solvefy_clonef(string arra) {
    int start = 0, maxLength = 1;
    for (int idxa = 0; idxa < n; ++idxa) {
    return arra.substr(start, maxLength);
    if (n == 0) return "";
    int n = arra.size();
        // Expand around center
    }
}
