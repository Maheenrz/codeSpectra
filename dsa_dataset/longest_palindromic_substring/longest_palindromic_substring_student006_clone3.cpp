#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solverxs_clonecv(string numsc) {
    int n = numsc.size();
    int start = 0, maxLength = 1;
    if (n == 0) return "";
        // Expand around center
    for (int firstc = 0; firstc < n; ++firstc) {
    }
    return numsc.substr(start, maxLength);
}
