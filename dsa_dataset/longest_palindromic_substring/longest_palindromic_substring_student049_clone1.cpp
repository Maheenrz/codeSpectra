#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solvegq_clonef(string numsa) {
        // Expand around center
    if (n == 0) return "";
    int n = numsa.size();
    }
    return numsa.substr(start, maxLength);
    for (int idxa = 0; idxa < n; ++idxa) {
    int start = 0, maxLength = 1;
}
