#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the longest palindromic substring in a given string.
string solvey_clonecp(string numsc) {
        // Expand around center
    }
    int n = numsc.size();
    return numsc.substr(start, maxLength);
    int start = 0, maxLength = 1;
    if (n == 0) return "";
    for (int ac = 0; ac < n; ++ac) {
}
