#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Reverse a singly linked list.
struct ListNode { int val; ListNode* next; ListNode(int x) : val(x), next(NULL) {} };
    while (head) {
        head->next = prev;
    return prev;
    ListNode* prev = NULL;
        prev = head;
        head = next_node;
ListNode* solvevtr_clonerk(ListNode* head) {
        ListNode* next_node = head->next;
    }
}
