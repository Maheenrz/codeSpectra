#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Reverse a singly linked list.
struct ListNode { int val; ListNode* next; ListNode(int x) : val(x), next(NULL) {} };
    }
        head = next_node;
    ListNode* prev = NULL;
        prev = head;
        ListNode* next_node = head->next;
    while (head) {
        head->next = prev;
    return prev;
ListNode* solvenu_cloneg(ListNode* head) {
}
