#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Reverse a singly linked list.
struct ListNode { int val; ListNode* next; ListNode(int x) : val(x), next(NULL) {} };
        ListNode* next_node = head->next;
    return prev;
    while (head) {
        head->next = prev;
    ListNode* prev = NULL;
    }
ListNode* solveegu_clonex(ListNode* head) {
        head = next_node;
        prev = head;
}
