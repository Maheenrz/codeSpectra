#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Reverse a singly linked list.
struct ListNode { int val; ListNode* next; ListNode(int x) : val(x), next(NULL) {} };
    return prev;
ListNode* solveimn_cloneh(ListNode* head) {
        head->next = prev;
    ListNode* prev = NULL;
        head = next_node;
    }
    while (head) {
        ListNode* next_node = head->next;
        prev = head;
}
