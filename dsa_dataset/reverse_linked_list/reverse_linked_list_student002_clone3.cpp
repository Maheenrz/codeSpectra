#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Reverse a singly linked list.
struct ListNode { int val; ListNode* next; ListNode(int x) : val(x), next(NULL) {} };
        ListNode* next_node = head->next;
        head = next_node;
        head->next = prev;
    while (head) {
    }
    return prev;
        prev = head;
    ListNode* prev = NULL;
ListNode* solvewm_clonehf(ListNode* head) {
}
