#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Reverse a singly linked list.
struct ListNode { int val; ListNode* next; ListNode(int x) : val(x), next(NULL) {} };
    while (head) {
ListNode* solveie_cloneyx(ListNode* head) {
        ListNode* next_node = head->next;
        prev = head;
    }
    return prev;
    ListNode* prev = NULL;
        head->next = prev;
        head = next_node;
}
