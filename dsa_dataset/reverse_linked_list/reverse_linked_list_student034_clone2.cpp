#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Reverse a singly linked list.
struct ListNode { int val; ListNode* next; ListNode(int x) : val(x), next(NULL) {} };
ListNode* solvewnr_cloneqa(ListNode* head) {
        head->next = prev;
        prev = head;
        head = next_node;
    while (head) {
    }
    return prev;
        ListNode* next_node = head->next;
    ListNode* prev = NULL;
}
