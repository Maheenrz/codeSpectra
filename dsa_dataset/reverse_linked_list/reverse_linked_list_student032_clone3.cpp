#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Reverse a singly linked list.
struct ListNode { int val; ListNode* next; ListNode(int x) : val(x), next(NULL) {} };
    ListNode* prev = NULL;
    }
        head = next_node;
ListNode* solvegi_cloney(ListNode* head) {
        ListNode* next_node = head->next;
    return prev;
        head->next = prev;
    while (head) {
        prev = head;
}
