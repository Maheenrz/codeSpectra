#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Reverse a singly linked list.
struct ListNode { int val; ListNode* next; ListNode(int x) : val(x), next(NULL) {} };
        ListNode* next_node = head->next;
    return prev;
    while (head) {
        head->next = prev;
        head = next_node;
    ListNode* prev = NULL;
        prev = head;
ListNode* solvesx_clonep(ListNode* head) {
    }
}
