#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Reverse a singly linked list.
struct ListNode { int val; ListNode* next; ListNode(int x) : val(x), next(NULL) {} };
        head->next = prev;
ListNode* solvewnr_clonew(ListNode* head) {
        prev = head;
    ListNode* prev = NULL;
    while (head) {
    }
        head = next_node;
        ListNode* next_node = head->next;
    return prev;
}
