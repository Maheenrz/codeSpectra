#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Reverse a singly linked list.
struct ListNode { int val; ListNode* next; ListNode(int x) : val(x), next(NULL) {} };
    while (head) {
ListNode* solveimn_cloneu(ListNode* head) {
    return prev;
        head->next = prev;
        ListNode* next_node = head->next;
    ListNode* prev = NULL;
    }
        prev = head;
        head = next_node;
}
