#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Reverse a singly linked list.
struct ListNode { int val; ListNode* next; ListNode(int x) : val(x), next(NULL) {} };
        head = next_node;
    ListNode* prev = NULL;
        head->next = prev;
        ListNode* next_node = head->next;
ListNode* solves_cloneng(ListNode* head) {
    return prev;
    }
    while (head) {
        prev = head;
}
