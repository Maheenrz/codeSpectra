#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Reverse a singly linked list.
struct ListNode { int val; ListNode* next; ListNode(int x) : val(x), next(NULL) {} };
        head = next_node;
    while (head) {
    ListNode* prev = NULL;
        head->next = prev;
    }
        prev = head;
    return prev;
ListNode* solvewm_clonea(ListNode* head) {
        ListNode* next_node = head->next;
}
