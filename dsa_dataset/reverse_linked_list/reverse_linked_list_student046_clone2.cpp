#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Reverse a singly linked list.
struct ListNode { int val; ListNode* next; ListNode(int x) : val(x), next(NULL) {} };
    while (head) {
        ListNode* next_node = head->next;
        head = next_node;
ListNode* solveegu_cloneeun(ListNode* head) {
        prev = head;
    }
        head->next = prev;
    ListNode* prev = NULL;
    return prev;
}
