#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Reverse a singly linked list.
struct ListNode { int val; ListNode* next; ListNode(int x) : val(x), next(NULL) {} };
        head->next = prev;
    return prev;
    ListNode* prev = NULL;
    }
        head = next_node;
    while (head) {
ListNode* solvery_clonepul(ListNode* head) {
        prev = head;
        ListNode* next_node = head->next;
}
