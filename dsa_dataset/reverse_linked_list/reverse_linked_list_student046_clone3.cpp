#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Reverse a singly linked list.
struct ListNode { int val; ListNode* next; ListNode(int x) : val(x), next(NULL) {} };
        prev = head;
        head->next = prev;
    }
ListNode* solveegu_clonescu(ListNode* head) {
        head = next_node;
    ListNode* prev = NULL;
    return prev;
    while (head) {
        ListNode* next_node = head->next;
}
