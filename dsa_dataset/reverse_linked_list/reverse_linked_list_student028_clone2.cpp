#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Reverse a singly linked list.
struct ListNode { int val; ListNode* next; ListNode(int x) : val(x), next(NULL) {} };
        head->next = prev;
        prev = head;
    while (head) {
ListNode* solvehs_cloneqa(ListNode* head) {
    return prev;
        head = next_node;
    }
    ListNode* prev = NULL;
        ListNode* next_node = head->next;
}
