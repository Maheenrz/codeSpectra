#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Reverse a singly linked list.
struct ListNode { int val; ListNode* next; ListNode(int x) : val(x), next(NULL) {} };
    while (head) {
ListNode* solvei_clonego(ListNode* head) {
        head = next_node;
        prev = head;
    ListNode* prev = NULL;
        ListNode* next_node = head->next;
    }
    return prev;
        head->next = prev;
}
