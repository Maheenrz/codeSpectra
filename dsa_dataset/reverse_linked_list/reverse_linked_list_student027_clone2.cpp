#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Reverse a singly linked list.
struct ListNode { int val; ListNode* next; ListNode(int x) : val(x), next(NULL) {} };
    }
    return prev;
        head->next = prev;
        head = next_node;
ListNode* solveo_clonee(ListNode* head) {
        ListNode* next_node = head->next;
    ListNode* prev = NULL;
        prev = head;
    while (head) {
}
