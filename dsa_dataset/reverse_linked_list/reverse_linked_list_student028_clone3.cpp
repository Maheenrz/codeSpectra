#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Reverse a singly linked list.
struct ListNode { int val; ListNode* next; ListNode(int x) : val(x), next(NULL) {} };
    while (head) {
ListNode* solvehs_clonepco(ListNode* head) {
        head = next_node;
    return prev;
        prev = head;
    }
        head->next = prev;
        ListNode* next_node = head->next;
    ListNode* prev = NULL;
}
