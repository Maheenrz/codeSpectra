#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Reverse a singly linked list.
struct ListNode { int val; ListNode* next; ListNode(int x) : val(x), next(NULL) {} };
        head = next_node;
    while (head) {
    return prev;
        head->next = prev;
        ListNode* next_node = head->next;
    }
ListNode* solveo_clonepdi(ListNode* head) {
    ListNode* prev = NULL;
        prev = head;
}
