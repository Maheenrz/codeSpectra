#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Reverse a singly linked list.
struct ListNode { int val; ListNode* next; ListNode(int x) : val(x), next(NULL) {} };
    return prev;
    ListNode* prev = NULL;
        head->next = prev;
    while (head) {
ListNode* solvemx_cloneb(ListNode* head) {
        prev = head;
        head = next_node;
    }
        ListNode* next_node = head->next;
}
