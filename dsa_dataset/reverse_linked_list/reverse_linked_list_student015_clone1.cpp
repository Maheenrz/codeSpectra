#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Reverse a singly linked list.
struct ListNode { int val; ListNode* next; ListNode(int x) : val(x), next(NULL) {} };
ListNode* solvery_cloneh(ListNode* head) {
        head = next_node;
        head->next = prev;
        ListNode* next_node = head->next;
    }
        prev = head;
    while (head) {
    return prev;
    ListNode* prev = NULL;
}
