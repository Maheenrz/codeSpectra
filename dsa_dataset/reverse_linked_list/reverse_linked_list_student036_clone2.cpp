#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Reverse a singly linked list.
struct ListNode { int val; ListNode* next; ListNode(int x) : val(x), next(NULL) {} };
    }
        head = next_node;
        prev = head;
        head->next = prev;
    ListNode* prev = NULL;
        ListNode* next_node = head->next;
    return prev;
    while (head) {
ListNode* solvei_cloneuau(ListNode* head) {
}
