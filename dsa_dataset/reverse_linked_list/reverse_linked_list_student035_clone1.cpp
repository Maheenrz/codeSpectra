#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Reverse a singly linked list.
struct ListNode { int val; ListNode* next; ListNode(int x) : val(x), next(NULL) {} };
        prev = head;
    while (head) {
        head->next = prev;
    ListNode* prev = NULL;
        head = next_node;
        ListNode* next_node = head->next;
    return prev;
    }
ListNode* solvemit_clonersu(ListNode* head) {
}
