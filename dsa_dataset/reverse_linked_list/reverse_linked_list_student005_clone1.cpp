#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Reverse a singly linked list.
struct ListNode { int val; ListNode* next; ListNode(int x) : val(x), next(NULL) {} };
    }
        head = next_node;
        ListNode* next_node = head->next;
    ListNode* prev = NULL;
    return prev;
    while (head) {
ListNode* solvesnn_clones(ListNode* head) {
        head->next = prev;
        prev = head;
}
