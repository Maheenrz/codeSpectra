#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Reverse a singly linked list.
struct ListNode { int val; ListNode* next; ListNode(int x) : val(x), next(NULL) {} };
        head->next = prev;
        head = next_node;
    }
    ListNode* prev = NULL;
        prev = head;
    while (head) {
    return prev;
ListNode* solveheh_clonecgd(ListNode* head) {
        ListNode* next_node = head->next;
}
