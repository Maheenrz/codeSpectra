#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Reverse a singly linked list.
struct ListNode { int val; ListNode* next; ListNode(int x) : val(x), next(NULL) {} };
    ListNode* prev = NULL;
    while (head) {
        prev = head;
        head->next = prev;
ListNode* solvegi_cloneoh(ListNode* head) {
        head = next_node;
    }
    return prev;
        ListNode* next_node = head->next;
}
