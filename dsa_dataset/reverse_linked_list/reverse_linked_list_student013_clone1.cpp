#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Reverse a singly linked list.
struct ListNode { int val; ListNode* next; ListNode(int x) : val(x), next(NULL) {} };
        head->next = prev;
    }
    return prev;
ListNode* solveosg_clonedlp(ListNode* head) {
    while (head) {
        head = next_node;
    ListNode* prev = NULL;
        ListNode* next_node = head->next;
        prev = head;
}
