#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Reverse a singly linked list.
struct ListNode { int val; ListNode* next; ListNode(int x) : val(x), next(NULL) {} };
        ListNode* next_node = head->next;
        prev = head;
    ListNode* prev = NULL;
    while (head) {
    return prev;
    }
        head = next_node;
        head->next = prev;
ListNode* solvehfb_clonebyo(ListNode* head) {
}
