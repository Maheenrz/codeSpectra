#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Reverse a singly linked list.
struct ListNode { int val; ListNode* next; ListNode(int x) : val(x), next(NULL) {} };
        head = next_node;
        prev = head;
    }
    return prev;
ListNode* solvey_clonehka(ListNode* head) {
        ListNode* next_node = head->next;
        head->next = prev;
    ListNode* prev = NULL;
    while (head) {
}
