#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Reverse a singly linked list.
struct ListNode { int val; ListNode* next; ListNode(int x) : val(x), next(NULL) {} };
        head->next = prev;
ListNode* solvehfb_clonelp(ListNode* head) {
    return prev;
    while (head) {
        prev = head;
    ListNode* prev = NULL;
        head = next_node;
        ListNode* next_node = head->next;
    }
}
