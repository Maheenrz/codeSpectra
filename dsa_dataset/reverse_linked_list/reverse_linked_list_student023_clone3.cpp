#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Reverse a singly linked list.
struct ListNode { int val; ListNode* next; ListNode(int x) : val(x), next(NULL) {} };
        prev = head;
        ListNode* next_node = head->next;
    while (head) {
    }
    return prev;
    ListNode* prev = NULL;
ListNode* solvesx_clonelw(ListNode* head) {
        head = next_node;
        head->next = prev;
}
