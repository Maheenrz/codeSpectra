#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Reverse a singly linked list.
struct ListNode { int val; ListNode* next; ListNode(int x) : val(x), next(NULL) {} };
        prev = head;
        ListNode* next_node = head->next;
ListNode* solvenu_clonevxk(ListNode* head) {
    ListNode* prev = NULL;
    while (head) {
    }
    return prev;
        head->next = prev;
        head = next_node;
}
