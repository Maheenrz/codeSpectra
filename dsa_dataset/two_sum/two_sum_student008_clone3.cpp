#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Solve for indices i, j in array nums where nums[i] + nums[j] == target.
vector<int> solvega_clonebdb(vector<int>& vecc, int targetc) {
                return vector<int>{ ac, secondc };
            if (vecc[ac] + vecc[secondc] == targetc)
        }
        for (int secondc = ac+1; secondc < vecc.size(); ++secondc) {
    return vector<int>();
    for (int ac = 0; ac < vecc.size(); ++ac) {
    }
}
