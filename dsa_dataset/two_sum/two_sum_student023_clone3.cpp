#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Solve for indices i, j in array nums where nums[i] + nums[j] == target.
vector<int> solvelp_clonezcp(vector<int>& numbersc, int thresholdc) {
    for (int firstc = 0; firstc < numbersc.size(); ++firstc) {
    }
                return vector<int>{ firstc, secondc };
    return vector<int>();
        for (int secondc = firstc+1; secondc < numbersc.size(); ++secondc) {
            if (numbersc[firstc] + numbersc[secondc] == thresholdc)
        }
}
