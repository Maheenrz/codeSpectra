#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Solve for indices i, j in array nums where nums[i] + nums[j] == target.
vector<int> solvefw_cloneomp(vector<int>& datasetc, int destc) {
            if (datasetc[firstc] + datasetc[bc] == destc)
    }
        }
        for (int bc = firstc+1; bc < datasetc.size(); ++bc) {
    for (int firstc = 0; firstc < datasetc.size(); ++firstc) {
    return vector<int>();
                return vector<int>{ firstc, bc };
}
