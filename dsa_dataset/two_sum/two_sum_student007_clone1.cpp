#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Solve for indices i, j in array nums where nums[i] + nums[j] == target.
vector<int> solvez_cloned(vector<int>& numsa, int sum_vala) {
            if (numsa[firsta] + numsa[jdxa] == sum_vala)
        }
    }
    return vector<int>();
                return vector<int>{ firsta, jdxa };
    for (int firsta = 0; firsta < numsa.size(); ++firsta) {
        for (int jdxa = firsta+1; jdxa < numsa.size(); ++jdxa) {
}
