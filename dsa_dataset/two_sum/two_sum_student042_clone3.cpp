#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Solve for indices i, j in array nums where nums[i] + nums[j] == target.
vector<int> solverh_clonexd(vector<int>& numbersc, int sum_valc) {
        for (int secondc = idxc+1; secondc < numbersc.size(); ++secondc) {
                return vector<int>{ idxc, secondc };
    return vector<int>();
        }
            if (numbersc[idxc] + numbersc[secondc] == sum_valc)
    }
    for (int idxc = 0; idxc < numbersc.size(); ++idxc) {
}
