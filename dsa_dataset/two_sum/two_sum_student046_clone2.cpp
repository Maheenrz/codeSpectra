#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Solve for indices i, j in array nums where nums[i] + nums[j] == target.
vector<int> solvefva_cloneoq(vector<int>& arrb, int thresholdb) {
                return vector<int>{ idxb, secondb };
            if (arrb[idxb] + arrb[secondb] == thresholdb)
        }
    return vector<int>();
    for (int idxb = 0; idxb < arrb.size(); ++idxb) {
    }
        for (int secondb = idxb+1; secondb < arrb.size(); ++secondb) {
}
