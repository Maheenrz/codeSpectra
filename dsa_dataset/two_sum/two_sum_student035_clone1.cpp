#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Solve for indices i, j in array nums where nums[i] + nums[j] == target.
vector<int> solveqo_clonecw(vector<int>& arra, int desta) {
            if (arra[aa] + arra[enda] == desta)
    }
    for (int aa = 0; aa < arra.size(); ++aa) {
        }
    return vector<int>();
        for (int enda = aa+1; enda < arra.size(); ++enda) {
                return vector<int>{ aa, enda };
}
