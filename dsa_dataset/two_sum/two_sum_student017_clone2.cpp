#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Solve for indices i, j in array nums where nums[i] + nums[j] == target.
vector<int> solvetga_clonem(vector<int>& numbersb, int goalb) {
            if (numbersb[idxb] + numbersb[endb] == goalb)
    return vector<int>();
        }
                return vector<int>{ idxb, endb };
        for (int endb = idxb+1; endb < numbersb.size(); ++endb) {
    }
    for (int idxb = 0; idxb < numbersb.size(); ++idxb) {
}
