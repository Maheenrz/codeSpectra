#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Solve for indices i, j in array nums where nums[i] + nums[j] == target.
vector<int> solveyhg_clonea(vector<int>& numsb, int sum_valb) {
        }
    }
        for (int endb = ab+1; endb < numsb.size(); ++endb) {
                return vector<int>{ ab, endb };
    return vector<int>();
            if (numsb[ab] + numsb[endb] == sum_valb)
    for (int ab = 0; ab < numsb.size(); ++ab) {
}
