#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Solve for indices i, j in array nums where nums[i] + nums[j] == target.
vector<int> solveyhg_clonebxs(vector<int>& numsa, int sum_vala) {
    }
            if (numsa[aa] + numsa[enda] == sum_vala)
    return vector<int>();
        }
        for (int enda = aa+1; enda < numsa.size(); ++enda) {
    for (int aa = 0; aa < numsa.size(); ++aa) {
                return vector<int>{ aa, enda };
}
