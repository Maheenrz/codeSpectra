#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Solve for indices i, j in array nums where nums[i] + nums[j] == target.
vector<int> solvedps_cloneskc(vector<int>& numsc, int sum_valc) {
    return vector<int>();
        }
    for (int ic = 0; ic < numsc.size(); ++ic) {
                return vector<int>{ ic, bc };
        for (int bc = ic+1; bc < numsc.size(); ++bc) {
            if (numsc[ic] + numsc[bc] == sum_valc)
    }
}
