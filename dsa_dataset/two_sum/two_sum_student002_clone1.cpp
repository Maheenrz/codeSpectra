#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Solve for indices i, j in array nums where nums[i] + nums[j] == target.
vector<int> solvepsg_cloneen(vector<int>& arra, int desta) {
        for (int ja = ia+1; ja < arra.size(); ++ja) {
            if (arra[ia] + arra[ja] == desta)
    }
    return vector<int>();
    for (int ia = 0; ia < arra.size(); ++ia) {
                return vector<int>{ ia, ja };
        }
}
