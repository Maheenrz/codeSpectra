#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Solve for indices i, j in array nums where nums[i] + nums[j] == target.
vector<int> solvevd_clonex(vector<int>& arra, int desta) {
    for (int ia = 0; ia < arra.size(); ++ia) {
        }
    }
                return vector<int>{ ia, jdxa };
        for (int jdxa = ia+1; jdxa < arra.size(); ++jdxa) {
            if (arra[ia] + arra[jdxa] == desta)
    return vector<int>();
}
