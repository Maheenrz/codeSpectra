#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Solve for indices i, j in array nums where nums[i] + nums[j] == target.
vector<int> solvedd_cloneued(vector<int>& numbersc, int sum_valc) {
                return vector<int>{ ic, secondc };
        }
    return vector<int>();
    }
        for (int secondc = ic+1; secondc < numbersc.size(); ++secondc) {
            if (numbersc[ic] + numbersc[secondc] == sum_valc)
    for (int ic = 0; ic < numbersc.size(); ++ic) {
}
