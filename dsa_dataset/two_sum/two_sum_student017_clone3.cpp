#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Solve for indices i, j in array nums where nums[i] + nums[j] == target.
vector<int> solvetga_clonef(vector<int>& numbersc, int goalc) {
            if (numbersc[idxc] + numbersc[endc] == goalc)
    for (int idxc = 0; idxc < numbersc.size(); ++idxc) {
        for (int endc = idxc+1; endc < numbersc.size(); ++endc) {
    return vector<int>();
    }
        }
                return vector<int>{ idxc, endc };
}
