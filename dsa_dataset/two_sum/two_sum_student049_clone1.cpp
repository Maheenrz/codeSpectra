#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Solve for indices i, j in array nums where nums[i] + nums[j] == target.
vector<int> solvew_cloneurr(vector<int>& arra, int goala) {
                return vector<int>{ idxa, ja };
    }
    return vector<int>();
        for (int ja = idxa+1; ja < arra.size(); ++ja) {
        }
            if (arra[idxa] + arra[ja] == goala)
    for (int idxa = 0; idxa < arra.size(); ++idxa) {
}
