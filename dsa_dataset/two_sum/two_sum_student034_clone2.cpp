#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Solve for indices i, j in array nums where nums[i] + nums[j] == target.
vector<int> solvetxp_clonea(vector<int>& numsb, int targetb) {
    for (int ab = 0; ab < numsb.size(); ++ab) {
    return vector<int>();
        }
                return vector<int>{ ab, endb };
            if (numsb[ab] + numsb[endb] == targetb)
        for (int endb = ab+1; endb < numsb.size(); ++endb) {
    }
}
