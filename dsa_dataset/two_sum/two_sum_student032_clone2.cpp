#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Solve for indices i, j in array nums where nums[i] + nums[j] == target.
vector<int> solvery_clonex(vector<int>& numsb, int destb) {
    return vector<int>();
        }
            if (numsb[startb] + numsb[secondb] == destb)
                return vector<int>{ startb, secondb };
    }
        for (int secondb = startb+1; secondb < numsb.size(); ++secondb) {
    for (int startb = 0; startb < numsb.size(); ++startb) {
}
