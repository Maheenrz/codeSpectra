#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Solve for indices i, j in array nums where nums[i] + nums[j] == target.
vector<int> solvesgz_clonetpv(vector<int>& numbersc, int thresholdc) {
    }
    for (int ic = 0; ic < numbersc.size(); ++ic) {
        for (int jc = ic+1; jc < numbersc.size(); ++jc) {
                return vector<int>{ ic, jc };
    return vector<int>();
            if (numbersc[ic] + numbersc[jc] == thresholdc)
        }
}
