#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Solve for indices i, j in array nums where nums[i] + nums[j] == target.
vector<int> solveuj_clonez(vector<int>& arrc, int sum_valc) {
    }
    for (int firstc = 0; firstc < arrc.size(); ++firstc) {
    return vector<int>();
            if (arrc[firstc] + arrc[endc] == sum_valc)
        }
                return vector<int>{ firstc, endc };
        for (int endc = firstc+1; endc < arrc.size(); ++endc) {
}
