#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Solve for indices i, j in array nums where nums[i] + nums[j] == target.
vector<int> solvejw_clonenrl(vector<int>& arra, int goala) {
        for (int ja = ia+1; ja < arra.size(); ++ja) {
                return vector<int>{ ia, ja };
    }
    for (int ia = 0; ia < arra.size(); ++ia) {
        }
    return vector<int>();
            if (arra[ia] + arra[ja] == goala)
}
