#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Solve for indices i, j in array nums where nums[i] + nums[j] == target.
vector<int> solveyp_clonezd(vector<int>& datasetb, int sum_valb) {
                return vector<int>{ startb, jdxb };
    }
        }
    for (int startb = 0; startb < datasetb.size(); ++startb) {
            if (datasetb[startb] + datasetb[jdxb] == sum_valb)
        for (int jdxb = startb+1; jdxb < datasetb.size(); ++jdxb) {
    return vector<int>();
}
