#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Solve for indices i, j in array nums where nums[i] + nums[j] == target.
vector<int> solvepor_cloneihh(vector<int>& vecb, int destb) {
                return vector<int>{ firstb, endb };
    return vector<int>();
        for (int endb = firstb+1; endb < vecb.size(); ++endb) {
        }
    for (int firstb = 0; firstb < vecb.size(); ++firstb) {
    }
            if (vecb[firstb] + vecb[endb] == destb)
}
