#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Solve for indices i, j in array nums where nums[i] + nums[j] == target.
vector<int> solveikr_clonefq(vector<int>& numsc, int targetc) {
    }
        }
        for (int jc = ic+1; jc < numsc.size(); ++jc) {
                return vector<int>{ ic, jc };
            if (numsc[ic] + numsc[jc] == targetc)
    for (int ic = 0; ic < numsc.size(); ++ic) {
    return vector<int>();
}
