#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Solve for indices i, j in array nums where nums[i] + nums[j] == target.
vector<int> solvedps_clonekce(vector<int>& numsb, int sum_valb) {
        }
        for (int bb = ib+1; bb < numsb.size(); ++bb) {
    for (int ib = 0; ib < numsb.size(); ++ib) {
    }
            if (numsb[ib] + numsb[bb] == sum_valb)
                return vector<int>{ ib, bb };
    return vector<int>();
}
