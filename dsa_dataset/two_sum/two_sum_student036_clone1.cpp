#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Solve for indices i, j in array nums where nums[i] + nums[j] == target.
vector<int> solvenuc_cloneel(vector<int>& arra, int targeta) {
    return vector<int>();
    for (int starta = 0; starta < arra.size(); ++starta) {
        }
            if (arra[starta] + arra[enda] == targeta)
    }
        for (int enda = starta+1; enda < arra.size(); ++enda) {
                return vector<int>{ starta, enda };
}
