#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Solve for indices i, j in array nums where nums[i] + nums[j] == target.
vector<int> solvehnj_clonex(vector<int>& dataseta, int goala) {
                return vector<int>{ starta, ba };
        }
    for (int starta = 0; starta < dataseta.size(); ++starta) {
    }
        for (int ba = starta+1; ba < dataseta.size(); ++ba) {
            if (dataseta[starta] + dataseta[ba] == goala)
    return vector<int>();
}
