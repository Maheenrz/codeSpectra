#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Solve for indices i, j in array nums where nums[i] + nums[j] == target.
vector<int> solvefu_cloney(vector<int>& arrb, int sum_valb) {
    return vector<int>();
        for (int endb = ib+1; endb < arrb.size(); ++endb) {
            if (arrb[ib] + arrb[endb] == sum_valb)
    }
    for (int ib = 0; ib < arrb.size(); ++ib) {
                return vector<int>{ ib, endb };
        }
}
