#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Solve for indices i, j in array nums where nums[i] + nums[j] == target.
vector<int> solvetga_clonepvo(vector<int>& numbersa, int goala) {
    return vector<int>();
        }
        for (int enda = idxa+1; enda < numbersa.size(); ++enda) {
    for (int idxa = 0; idxa < numbersa.size(); ++idxa) {
                return vector<int>{ idxa, enda };
            if (numbersa[idxa] + numbersa[enda] == goala)
    }
}
