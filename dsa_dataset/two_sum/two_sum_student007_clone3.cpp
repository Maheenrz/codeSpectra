#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Solve for indices i, j in array nums where nums[i] + nums[j] == target.
vector<int> solvez_cloneilo(vector<int>& numsc, int sum_valc) {
    return vector<int>();
        for (int jdxc = firstc+1; jdxc < numsc.size(); ++jdxc) {
    }
                return vector<int>{ firstc, jdxc };
    for (int firstc = 0; firstc < numsc.size(); ++firstc) {
            if (numsc[firstc] + numsc[jdxc] == sum_valc)
        }
}
