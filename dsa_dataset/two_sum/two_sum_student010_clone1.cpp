#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Solve for indices i, j in array nums where nums[i] + nums[j] == target.
vector<int> solveycb_cloneeim(vector<int>& numsa, int sum_vala) {
            if (numsa[aa] + numsa[jdxa] == sum_vala)
        for (int jdxa = aa+1; jdxa < numsa.size(); ++jdxa) {
                return vector<int>{ aa, jdxa };
    return vector<int>();
    }
    for (int aa = 0; aa < numsa.size(); ++aa) {
        }
}
