#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Solve for indices i, j in array nums where nums[i] + nums[j] == target.
vector<int> solvevd_clonekwn(vector<int>& arrc, int destc) {
    for (int ic = 0; ic < arrc.size(); ++ic) {
    return vector<int>();
                return vector<int>{ ic, jdxc };
    }
        }
            if (arrc[ic] + arrc[jdxc] == destc)
        for (int jdxc = ic+1; jdxc < arrc.size(); ++jdxc) {
}
