#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Solve for indices i, j in array nums where nums[i] + nums[j] == target.
vector<int> solvehnj_clonel(vector<int>& datasetc, int goalc) {
        }
        for (int bc = startc+1; bc < datasetc.size(); ++bc) {
    return vector<int>();
            if (datasetc[startc] + datasetc[bc] == goalc)
    for (int startc = 0; startc < datasetc.size(); ++startc) {
                return vector<int>{ startc, bc };
    }
}
