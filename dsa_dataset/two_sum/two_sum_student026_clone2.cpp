#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Solve for indices i, j in array nums where nums[i] + nums[j] == target.
vector<int> solvenx_clonel(vector<int>& numbersb, int targetb) {
        }
    }
                return vector<int>{ ab, jdxb };
    return vector<int>();
    for (int ab = 0; ab < numbersb.size(); ++ab) {
        for (int jdxb = ab+1; jdxb < numbersb.size(); ++jdxb) {
            if (numbersb[ab] + numbersb[jdxb] == targetb)
}
