#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Solve for indices i, j in array nums where nums[i] + nums[j] == target.
vector<int> solvexj_clonee(vector<int>& vecb, int destb) {
    return vector<int>();
                return vector<int>{ ab, bb };
        }
    for (int ab = 0; ab < vecb.size(); ++ab) {
    }
        for (int bb = ab+1; bb < vecb.size(); ++bb) {
            if (vecb[ab] + vecb[bb] == destb)
}
