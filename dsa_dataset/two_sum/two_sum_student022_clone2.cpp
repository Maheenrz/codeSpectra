#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Solve for indices i, j in array nums where nums[i] + nums[j] == target.
vector<int> solveb_cloneoif(vector<int>& listb, int sum_valb) {
            if (listb[firstb] + listb[bb] == sum_valb)
        }
    return vector<int>();
    for (int firstb = 0; firstb < listb.size(); ++firstb) {
    }
                return vector<int>{ firstb, bb };
        for (int bb = firstb+1; bb < listb.size(); ++bb) {
}
