#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Solve for indices i, j in array nums where nums[i] + nums[j] == target.
vector<int> solvekfg_clonex(vector<int>& veca, int goala) {
                return vector<int>{ starta, jdxa };
    return vector<int>();
        for (int jdxa = starta+1; jdxa < veca.size(); ++jdxa) {
            if (veca[starta] + veca[jdxa] == goala)
    for (int starta = 0; starta < veca.size(); ++starta) {
        }
    }
}
