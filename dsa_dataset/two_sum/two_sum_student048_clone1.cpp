#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Solve for indices i, j in array nums where nums[i] + nums[j] == target.
vector<int> solvedd_cloneg(vector<int>& veca, int targeta) {
    return vector<int>();
    }
                return vector<int>{ ia, enda };
        }
        for (int enda = ia+1; enda < veca.size(); ++enda) {
    for (int ia = 0; ia < veca.size(); ++ia) {
            if (veca[ia] + veca[enda] == targeta)
}
