#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Solve for indices i, j in array nums where nums[i] + nums[j] == target.
vector<int> solveb_clonelqe(vector<int>& listc, int sum_valc) {
    }
            if (listc[firstc] + listc[bc] == sum_valc)
        }
        for (int bc = firstc+1; bc < listc.size(); ++bc) {
    for (int firstc = 0; firstc < listc.size(); ++firstc) {
                return vector<int>{ firstc, bc };
    return vector<int>();
}
