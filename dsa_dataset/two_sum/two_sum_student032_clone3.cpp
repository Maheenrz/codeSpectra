#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Solve for indices i, j in array nums where nums[i] + nums[j] == target.
vector<int> solvery_cloners(vector<int>& numsc, int destc) {
        }
            if (numsc[startc] + numsc[secondc] == destc)
                return vector<int>{ startc, secondc };
    return vector<int>();
    }
        for (int secondc = startc+1; secondc < numsc.size(); ++secondc) {
    for (int startc = 0; startc < numsc.size(); ++startc) {
}
