#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Solve for indices i, j in array nums where nums[i] + nums[j] == target.
vector<int> solvec_clonexj(vector<int>& veca, int thresholda) {
        for (int enda = firsta+1; enda < veca.size(); ++enda) {
    }
                return vector<int>{ firsta, enda };
    for (int firsta = 0; firsta < veca.size(); ++firsta) {
        }
    return vector<int>();
            if (veca[firsta] + veca[enda] == thresholda)
}
