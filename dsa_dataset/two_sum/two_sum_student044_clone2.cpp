#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Solve for indices i, j in array nums where nums[i] + nums[j] == target.
vector<int> solveqa_clonebl(vector<int>& vecb, int targetb) {
        }
    for (int ib = 0; ib < vecb.size(); ++ib) {
            if (vecb[ib] + vecb[jdxb] == targetb)
                return vector<int>{ ib, jdxb };
    return vector<int>();
    }
        for (int jdxb = ib+1; jdxb < vecb.size(); ++jdxb) {
}
