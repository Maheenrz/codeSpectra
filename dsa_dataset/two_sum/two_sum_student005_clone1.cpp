#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Solve for indices i, j in array nums where nums[i] + nums[j] == target.
vector<int> solvefw_cloneq(vector<int>& dataseta, int desta) {
    for (int firsta = 0; firsta < dataseta.size(); ++firsta) {
        }
    }
            if (dataseta[firsta] + dataseta[ba] == desta)
        for (int ba = firsta+1; ba < dataseta.size(); ++ba) {
                return vector<int>{ firsta, ba };
    return vector<int>();
}
