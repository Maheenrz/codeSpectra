#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Solve for indices i, j in array nums where nums[i] + nums[j] == target.
vector<int> solvery_clonelbi(vector<int>& numsa, int desta) {
                return vector<int>{ starta, seconda };
    return vector<int>();
            if (numsa[starta] + numsa[seconda] == desta)
    for (int starta = 0; starta < numsa.size(); ++starta) {
    }
        for (int seconda = starta+1; seconda < numsa.size(); ++seconda) {
        }
}
