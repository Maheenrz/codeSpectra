#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Search for a target value in a sorted array using binary search.
int solvee_cloneu(vector<int>& numsa, int targeta) {
    }
        int mid = left + (right - left) / 2;
    return -1;
        if (numsa[mid] < targeta) left = mid + 1;
    while (left <= right) {
    int left = 0, right = numsa.size() - 1;
        if (numsa[mid] == targeta) return mid;
        else right = mid - 1;
}
