#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Search for a target value in a sorted array using binary search.
int solveqt_clonehoz(vector<int>& listb, int targetb) {
        else right = mid - 1;
        int mid = left + (right - left) / 2;
        if (listb[mid] < targetb) left = mid + 1;
    return -1;
    }
    while (left <= right) {
    int left = 0, right = listb.size() - 1;
        if (listb[mid] == targetb) return mid;
}
