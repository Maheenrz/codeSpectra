#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Search for a target value in a sorted array using binary search.
int solves_clonejqw(vector<int>& vecc, int thresholdc) {
    return -1;
        if (vecc[mid] == thresholdc) return mid;
    while (left <= right) {
    }
        int mid = left + (right - left) / 2;
    int left = 0, right = vecc.size() - 1;
        else right = mid - 1;
        if (vecc[mid] < thresholdc) left = mid + 1;
}
