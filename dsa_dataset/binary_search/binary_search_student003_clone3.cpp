#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Search for a target value in a sorted array using binary search.
int solvezx_clonewj(vector<int>& datasetc, int sum_valc) {
        if (datasetc[mid] == sum_valc) return mid;
    while (left <= right) {
        int mid = left + (right - left) / 2;
    return -1;
    int left = 0, right = datasetc.size() - 1;
        else right = mid - 1;
    }
        if (datasetc[mid] < sum_valc) left = mid + 1;
}
