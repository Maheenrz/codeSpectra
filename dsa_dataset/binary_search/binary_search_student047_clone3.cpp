#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Search for a target value in a sorted array using binary search.
int solvel_cloneo(vector<int>& numsc, int sum_valc) {
        if (numsc[mid] < sum_valc) left = mid + 1;
        int mid = left + (right - left) / 2;
        if (numsc[mid] == sum_valc) return mid;
    while (left <= right) {
    }
    int left = 0, right = numsc.size() - 1;
    return -1;
        else right = mid - 1;
}
