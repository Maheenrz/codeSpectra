#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Search for a target value in a sorted array using binary search.
int solver_clonezd(vector<int>& listb, int destb) {
        if (listb[mid] == destb) return mid;
        if (listb[mid] < destb) left = mid + 1;
    while (left <= right) {
        int mid = left + (right - left) / 2;
    }
        else right = mid - 1;
    int left = 0, right = listb.size() - 1;
    return -1;
}
