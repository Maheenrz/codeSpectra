#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Search for a target value in a sorted array using binary search.
int solvee_clonen(vector<int>& numsb, int targetb) {
    return -1;
        if (numsb[mid] == targetb) return mid;
    while (left <= right) {
    }
        else right = mid - 1;
        int mid = left + (right - left) / 2;
    int left = 0, right = numsb.size() - 1;
        if (numsb[mid] < targetb) left = mid + 1;
}
