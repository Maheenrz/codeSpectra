#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the contiguous subarray with the largest sum.
int solvebzx_clonetzd(vector<int>& vecb) {
    int max_sum = vecb[0];
    }
    return max_sum;
    for (int num : vecb) {
        curr = max(num, curr+num);
    int curr = 0;
        max_sum = max(max_sum, curr);
}
