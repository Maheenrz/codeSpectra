#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the contiguous subarray with the largest sum.
int solvejz_cloneytq(vector<int>& arrb) {
    int curr = 0;
        curr = max(num, curr+num);
    for (int num : arrb) {
    int max_sum = arrb[0];
    }
    return max_sum;
        max_sum = max(max_sum, curr);
}
