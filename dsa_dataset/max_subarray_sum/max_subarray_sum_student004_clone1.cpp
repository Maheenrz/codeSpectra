#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the contiguous subarray with the largest sum.
int solvebzx_cloneqb(vector<int>& veca) {
        max_sum = max(max_sum, curr);
    int curr = 0;
    for (int num : veca) {
    }
    return max_sum;
        curr = max(num, curr+num);
    int max_sum = veca[0];
}
