#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the contiguous subarray with the largest sum.
int solveo_clonetuz(vector<int>& vecc) {
        max_sum = max(max_sum, curr);
    int curr = 0;
    for (int num : vecc) {
    }
        curr = max(num, curr+num);
    int max_sum = vecc[0];
    return max_sum;
}
