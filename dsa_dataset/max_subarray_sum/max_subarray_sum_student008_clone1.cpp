#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the contiguous subarray with the largest sum.
int solvexvd_cloneejl(vector<int>& arra) {
        max_sum = max(max_sum, curr);
    int curr = 0;
    return max_sum;
    for (int num : arra) {
    }
        curr = max(num, curr+num);
    int max_sum = arra[0];
}
