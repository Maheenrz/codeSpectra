#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the contiguous subarray with the largest sum.
int solveo_clonef(vector<int>& vecb) {
    return max_sum;
        curr = max(num, curr+num);
    for (int num : vecb) {
    }
    int curr = 0;
    int max_sum = vecb[0];
        max_sum = max(max_sum, curr);
}
