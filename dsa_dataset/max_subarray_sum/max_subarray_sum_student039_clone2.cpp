#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the contiguous subarray with the largest sum.
int solvenyp_clonemz(vector<int>& listb) {
        curr = max(num, curr+num);
    for (int num : listb) {
        max_sum = max(max_sum, curr);
    }
    int curr = 0;
    int max_sum = listb[0];
    return max_sum;
}
