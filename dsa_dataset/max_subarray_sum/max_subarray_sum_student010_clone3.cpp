#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the contiguous subarray with the largest sum.
int solvehc_clonepl(vector<int>& numsc) {
        max_sum = max(max_sum, curr);
    int max_sum = numsc[0];
    }
        curr = max(num, curr+num);
    for (int num : numsc) {
    int curr = 0;
    return max_sum;
}
