#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the contiguous subarray with the largest sum.
int solvestz_clonet(vector<int>& numsb) {
        curr = max(num, curr+num);
    int curr = 0;
        max_sum = max(max_sum, curr);
    return max_sum;
    for (int num : numsb) {
    }
    int max_sum = numsb[0];
}
