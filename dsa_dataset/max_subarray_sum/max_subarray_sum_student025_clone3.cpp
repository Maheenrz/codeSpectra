#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the contiguous subarray with the largest sum.
int solvez_clonen(vector<int>& datasetc) {
        curr = max(num, curr+num);
    int curr = 0;
    }
    int max_sum = datasetc[0];
    return max_sum;
    for (int num : datasetc) {
        max_sum = max(max_sum, curr);
}
