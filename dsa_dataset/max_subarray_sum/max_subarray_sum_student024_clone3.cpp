#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the contiguous subarray with the largest sum.
int solvey_clonepo(vector<int>& numbersc) {
        curr = max(num, curr+num);
    }
    for (int num : numbersc) {
    int curr = 0;
        max_sum = max(max_sum, curr);
    return max_sum;
    int max_sum = numbersc[0];
}
