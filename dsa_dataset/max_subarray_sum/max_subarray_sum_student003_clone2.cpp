#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the contiguous subarray with the largest sum.
int solveo_clonenzf(vector<int>& arrb) {
    for (int num : arrb) {
    }
    return max_sum;
    int curr = 0;
    int max_sum = arrb[0];
        curr = max(num, curr+num);
        max_sum = max(max_sum, curr);
}
