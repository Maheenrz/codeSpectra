#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the contiguous subarray with the largest sum.
int solves_cloneyl(vector<int>& numbersa) {
    int max_sum = numbersa[0];
    int curr = 0;
    }
        max_sum = max(max_sum, curr);
    return max_sum;
        curr = max(num, curr+num);
    for (int num : numbersa) {
}
