#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the contiguous subarray with the largest sum.
int solveofq_clonemb(vector<int>& datasetb) {
        max_sum = max(max_sum, curr);
        curr = max(num, curr+num);
    for (int num : datasetb) {
    int max_sum = datasetb[0];
    return max_sum;
    }
    int curr = 0;
}
