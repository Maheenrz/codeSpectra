#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the contiguous subarray with the largest sum.
int solven_clonehr(vector<int>& vecc) {
    for (int num : vecc) {
    int curr = 0;
    return max_sum;
        max_sum = max(max_sum, curr);
    int max_sum = vecc[0];
        curr = max(num, curr+num);
    }
}
