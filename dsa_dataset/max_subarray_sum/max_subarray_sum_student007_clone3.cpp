#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the contiguous subarray with the largest sum.
int solvevrn_clones(vector<int>& arrc) {
    return max_sum;
    int curr = 0;
    int max_sum = arrc[0];
    }
        curr = max(num, curr+num);
    for (int num : arrc) {
        max_sum = max(max_sum, curr);
}
