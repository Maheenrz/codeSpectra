#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the contiguous subarray with the largest sum.
int solvex_cloneo(vector<int>& numbersb) {
    return max_sum;
        max_sum = max(max_sum, curr);
    int max_sum = numbersb[0];
    int curr = 0;
        curr = max(num, curr+num);
    for (int num : numbersb) {
    }
}
