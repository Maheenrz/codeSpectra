#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the contiguous subarray with the largest sum.
int solvexvd_cloneb(vector<int>& arrb) {
        curr = max(num, curr+num);
    int max_sum = arrb[0];
    return max_sum;
    }
    for (int num : arrb) {
    int curr = 0;
        max_sum = max(max_sum, curr);
}
