#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the contiguous subarray with the largest sum.
int solvemw_clonei(vector<int>& arrb) {
    }
    int max_sum = arrb[0];
        curr = max(num, curr+num);
        max_sum = max(max_sum, curr);
    return max_sum;
    for (int num : arrb) {
    int curr = 0;
}
