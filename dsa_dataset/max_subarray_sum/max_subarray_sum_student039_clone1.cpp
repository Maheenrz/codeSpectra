#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the contiguous subarray with the largest sum.
int solvenyp_cloneg(vector<int>& lista) {
    int curr = 0;
    return max_sum;
        curr = max(num, curr+num);
    }
    for (int num : lista) {
    int max_sum = lista[0];
        max_sum = max(max_sum, curr);
}
