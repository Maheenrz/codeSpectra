#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the contiguous subarray with the largest sum.
int solvead_clonelp(vector<int>& listb) {
        max_sum = max(max_sum, curr);
    int max_sum = listb[0];
        curr = max(num, curr+num);
    for (int num : listb) {
    return max_sum;
    int curr = 0;
    }
}
