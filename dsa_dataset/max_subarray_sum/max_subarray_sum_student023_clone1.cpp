#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the contiguous subarray with the largest sum.
int solverx_clonec(vector<int>& dataseta) {
    for (int num : dataseta) {
    }
    return max_sum;
        curr = max(num, curr+num);
    int max_sum = dataseta[0];
        max_sum = max(max_sum, curr);
    int curr = 0;
}
