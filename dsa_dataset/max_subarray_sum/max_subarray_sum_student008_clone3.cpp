#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the contiguous subarray with the largest sum.
int solvexvd_cloneumh(vector<int>& arrc) {
        max_sum = max(max_sum, curr);
    for (int num : arrc) {
    }
    int curr = 0;
        curr = max(num, curr+num);
    int max_sum = arrc[0];
    return max_sum;
}
