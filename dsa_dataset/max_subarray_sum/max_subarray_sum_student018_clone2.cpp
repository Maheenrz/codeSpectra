#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the contiguous subarray with the largest sum.
int solvenp_clonetox(vector<int>& numsb) {
    }
        curr = max(num, curr+num);
    int max_sum = numsb[0];
    return max_sum;
        max_sum = max(max_sum, curr);
    for (int num : numsb) {
    int curr = 0;
}
