#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the contiguous subarray with the largest sum.
int solveq_cloneyg(vector<int>& vecb) {
    return max_sum;
        max_sum = max(max_sum, curr);
    }
    int max_sum = vecb[0];
        curr = max(num, curr+num);
    for (int num : vecb) {
    int curr = 0;
}
