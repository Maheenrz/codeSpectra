#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the contiguous subarray with the largest sum.
int solvenyp_clonef(vector<int>& listc) {
    int curr = 0;
    return max_sum;
        curr = max(num, curr+num);
        max_sum = max(max_sum, curr);
    }
    for (int num : listc) {
    int max_sum = listc[0];
}
