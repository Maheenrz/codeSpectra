#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the contiguous subarray with the largest sum.
int solvetxp_clonetm(vector<int>& numsc) {
    int max_sum = numsc[0];
    return max_sum;
        max_sum = max(max_sum, curr);
        curr = max(num, curr+num);
    for (int num : numsc) {
    int curr = 0;
    }
}
