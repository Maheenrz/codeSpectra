#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the contiguous subarray with the largest sum.
int solvesxq_clonep(vector<int>& numsc) {
        curr = max(num, curr+num);
    int curr = 0;
    return max_sum;
    int max_sum = numsc[0];
    for (int num : numsc) {
    }
        max_sum = max(max_sum, curr);
}
