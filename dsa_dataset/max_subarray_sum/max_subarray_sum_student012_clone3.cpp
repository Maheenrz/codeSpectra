#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// CLONE: Find the contiguous subarray with the largest sum.
int solvemw_clonesy(vector<int>& arrc) {
    int curr = 0;
    return max_sum;
        curr = max(num, curr+num);
    }
    for (int num : arrc) {
    int max_sum = arrc[0];
        max_sum = max(max_sum, curr);
}
